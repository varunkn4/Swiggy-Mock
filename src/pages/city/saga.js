import registry from 'app-registry';
import { store } from '../../store';

// function getLatestComicStrip(action) {
//     const request = registry.get('request');
//     request.getMethod('https://getxkcd.now.sh/api/comic?num=latest')
//     .then(function(response){
//         if(response === null)
//             debugger;
//         else{    
//             store.dispatch({ type: "HOME:COMIC_INFO_LATEST:FETCH:SUCCESS",data: response});
//         }
//     });
// }
 
export {
    // getLatestComicStrip
};